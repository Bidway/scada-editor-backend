# Секреты
.env
.env.*
!.env.example

# Gradle / сборка
.gradle/
build/
bin/
out/
!gradle/wrapper/gradle-wrapper.jar

# Node (tools/kafka-sim и пр.)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python virtualenv (tools/plc-sim-venv) — машинозависимые бинарники, не для git
tools/plc-sim-venv/

# IDE
.idea/
*.iml
.vscode/
.settings/

# OS
.DS_Store
Thumbs.db

# Логи
*.log

# Docker-образы (локально в deliver/images/, не в репозитории)
deliver/images/*.tar.gz


# Документация и рабочие заметки — только локально
docs/
CLAUDE.md

# Локальный инструментарий и его данные: в репозитории не нужны, у каждого свои
.dolt/
*.db
# База задач вместе с конфигом — целиком локальная (16.09.2026): в репозитории ей не место,
# машина одна, перенос — через bd backup, а не через git.
.beads/
.beads-credential-key
# Скилы и метод разбора тоже только на этой машине.
.claude/
.superpowers/
# Разовые SQL и утилиты разбора чужих форматов — рабочий инструментарий, не часть сервиса.
scripts/
# Скриншоты стенда в корне — рабочие заметки; картинки сервисов лежат в их resources.
/*.png
.codex/
.agents/
.cursor/
/.beads/
/.playwright-mcp/
/tools/
