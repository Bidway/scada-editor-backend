package com.example.editor.model.version;

/**
 * Откуда взялась версия. Различать обязательно: инженер за смену жмёт «Сохранить» пару раз, а
 * таймер срабатывает три десятка раз, и «отменить последнее действие» по автосохранению
 * откатило бы то, чего человек не делал.
 */
public enum VersionKind {
    MANUAL,
    AUTOSAVE,
    RESTORE
}
