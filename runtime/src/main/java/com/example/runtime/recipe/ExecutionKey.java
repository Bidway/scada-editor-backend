package com.example.runtime.recipe;

/**
 * Ключ исполняющейся процедуры. Проект, а не сессия: мойка не принадлежит открытому экрану и
 * обязана пережить и уход оператора, и перезапуск сервиса. Один и тот же рецепт нельзя
 * запустить в проекте дважды — это и отбивается 409 на повторный start.
 */
record ExecutionKey(Long projectId, String recipeId) {
}
