package com.example.runtime.kafka;

/**
 * В проекте изменилось значение тега. Публикуется один раз на проект, независимо от того,
 * смотрит ли кто-нибудь: условия процедур обязаны пересчитываться и без открытых мониторов.
 * Прежнее событие по сессии осталось для доставки кадров наблюдателям.
 */
public record ProjectTagChangedEvent(Long projectId) {
}
