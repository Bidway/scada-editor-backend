# SCADA — демо редактора мнемосхем

Урезанная ветка: только то, что нужно для запуска редактора (`auth`, `editor`, `gateway`,
`script-core`). Базы каналов, монитора и runtime здесь нет.

Сборка архива и все команды для Linux-сервера — [`demo/README.md`](demo/README.md).
